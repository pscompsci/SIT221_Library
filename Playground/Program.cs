using System;

namespace Playground
{
    class Program
    {
        // static void Main(string[] args)
        // {
        //     // Checking IEnumerable iteration is read-only
            
        //     int[] arr = { 1, 2, 3, 4, 5, 6 };
        //     // foreach (int i in arr) i++; // error since i cannot be assigned to in a foreach
        //     foreach (int i in arr) Console.WriteLine(i);
        // }
    }
}
